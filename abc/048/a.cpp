#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep(i,n) for (int i = 0; i < (n); ++i)

int main() {
    string a, s, c;
    cin >> a >> s >> c;
    cout << a[0] << s[0] << c[0] << endl;
}
